1. Expand test coverage in `src/test/engine/systems/productionEngine.test.ts` to include edge cases for `calculateChemistry` and `transitionToProduction`.\n   - Test `calculateChemistry` with talent having negative stats (e.g., negative ego, negative draw).\n   - Test `transitionToProduction` with a project having a negative budget.\n   - Test `processProductionTick` for placeholder function execution.\n   - Test `transitionToProduction` adding `extraProjectUpdates`.\n2. Complete pre commit steps to make sure proper testing, verifications, reviews and reflections are done.\n3. Submit the change with a descriptive commit message.
